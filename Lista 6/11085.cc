#include <iostream>
#include <vector>

using namespace std;

vector<vector<int>> board{8,vector<int>(8)};

template <typename T>
void clear(){
	for (int i = 0 ; i < 8 ; i++)
		for (int j = 0 ; j < 8 ; j++)
			board[i][j] = 0;
}

int checkQ(int pos){
	if 
};

int main () {
	
	return 0;
}
